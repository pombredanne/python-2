version: 2
collectors:

- type: dependencies-component

- type: dockerfile
  path: Dockerfile
  manifest_updates:
    actors:
    - versions_regex: '^3\.\d\.\d-alpine$'

- type: python
  path: Pipfile
