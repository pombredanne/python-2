version: 2
dependencies:

- type: dependencies-component

- type: dockerfile
  path: Dockerfile
  manifest_updates:
    filters:
    - name: '.*'
      versions: L.Y.Y-alpine

- type: python
  path: Pipfile
